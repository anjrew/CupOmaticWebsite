(function () {

    var fadeIn = [
        'fadeInLeft',
        'fadeInUp',
        'fadeInDown',
        'fadeInRight'
    ];

    var fadeInLefts = document.getElementsByclass('fadeInLeft');
    var fadeInUps = document.getElementsByclass('fadeInUp');
    var fadeInDowns = document.getElementsByclass('fadeInDown');
    var fadeInRights = document.getElementsByclass('fadeInRight');

    function setup(){
        
        setUpFades(fadeInLefts, fadeIn[i]);
    }


    if ($('#element').visible(true)) {
        // The element is visible, do something
    } else {
        // The element is NOT visible, do something else
    }

})();